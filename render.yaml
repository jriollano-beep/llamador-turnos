# Configuración automática para Render.
# Con este archivo, Render detecta todo solo: no tienes que escribir comandos.
services:
  - type: web
    name: llamador-turnos
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: node server.js
